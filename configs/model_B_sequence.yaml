# Model configuration for PINN Direction B – Sequence Transformer
# [PINN_V2][2025-11-18][Direction B]

model:
  type: sequence
  context_embedding_dim: 64
  fourier_features: 8
  d_model: 128
  n_layers: 4
  n_heads: 4
  dim_feedforward: 512
  transformer_activation: gelu
  dropout: 0.05


